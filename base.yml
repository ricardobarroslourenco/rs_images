name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - xarray
  - matplotlib
  - scikit-learn
  - scikit-image
  - jupyterlab
  - pip
  - pip:
    - zenodo_get
    - netcdf4
prefix: /opt/conda
