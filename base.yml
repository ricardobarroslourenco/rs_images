name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - xarray
  - matplotlib
  - scikit-learn
  - scikit-image
  - jupyterlab
  - conda
  - mamba
  - pip
  - pip:
    - zenodo_get
    - netcdf4
prefix: /opt/conda
